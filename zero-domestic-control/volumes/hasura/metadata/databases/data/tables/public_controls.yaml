table:
  name: controls
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - room_id
        - type
        - name
      filter:
        room_id: { _eq: X-Hasura-Cabin }
      allow_aggregations: false
    comment: ""
