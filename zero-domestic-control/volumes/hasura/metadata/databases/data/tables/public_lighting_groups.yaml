table:
  name: lighting_groups
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - room_id
        - level
        - id
        - name
      filter:
        room_id: { _eq: X-Hasura-Cabin }
      allow_aggregations: true
    comment: ""
