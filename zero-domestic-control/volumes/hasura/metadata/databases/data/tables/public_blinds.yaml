table:
  name: blinds
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - room_id
        - level
        - id
        - name
        - group
        - opacity
      filter:
        room_id: { _eq: X-Hasura-Cabin }
      allow_aggregations: true
    comment: ""
  - role: admin
    permission:
      columns:
        - room_id
        - level
        - id
        - name
        - group
        - opacity
      filter: {}
      allow_aggregations: true
    comment: ""
