name: "zero_data_greptime"
version: "1.0.0"
config-version: 2

profile: "zero_data_greptime"

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

require-dbt-version: [">=1.10.0", "<2.0.0"]

# Models are views over raw `public` tables, materialized into the `views` database.
models:
  zero_data_greptime:
    staging:
      +materialized: view
