apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "..fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels: {{- include "..labels" . | nindent 4 }}
data:
  env.json: {{ .Values.ui.config | toJson | quote }}
