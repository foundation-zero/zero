# Zero Power Tags

This contains the code to handle reading the power tags from the Excel docs.

Since the Excels are ridden with typos we patch some of them in `patches.json`.

Otherwise this follows standard project conventions.
