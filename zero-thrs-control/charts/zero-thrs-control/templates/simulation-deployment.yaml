{{- if .Values.simulation.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zero-thrs-control.simulation" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zero-thrs-control.labels" . | nindent 4 }}
    app.kubernetes.io/part-of: thrs-control
    app.kubernetes.io/component: simulation
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "zero-thrs-control.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/part-of: thrs-control
      app.kubernetes.io/component: simulation
  template:
    metadata:
      labels:
        {{- include "zero-thrs-control.labels" . | nindent 8 }}
        app.kubernetes.io/part-of: thrs-control
        app.kubernetes.io/component: simulation
        thrs.zero.nl/mode: {{ .Values.simulation.type }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: thrs-control
          args:
           - "simulation"
           - "--mode"
           - "{{ .Values.simulation.type }}"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          image: "{{ .Values.control.image.repository }}:{{ .Values.control.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.control.image.pullPolicy }}
          resources:
            {{- toYaml .Values.simulation.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "zero-thrs-control.envvars" . }}
          volumeMounts:
            - mountPath: /tmp
              name: tmp-volume
          livenessProbe:
            {{- include "zero-thrs-control.livenessProbe" .Values.livenessPath | nindent 12 }}
      volumes:
        - name: tmp-volume
          emptyDir: {}
{{- end }}
