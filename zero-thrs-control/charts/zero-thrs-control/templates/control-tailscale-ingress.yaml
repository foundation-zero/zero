{{- if and .Values.control.enabled .Values.control.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "zero-thrs-control.control" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zero-thrs-control.labels" . | nindent 4 }}
spec:
  defaultBackend:
    service:
      name: {{ include "zero-thrs-control.control" . }}
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - {{ .Values.control.ingress.hostname }}
{{- end }}
