apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: kiosk-critical
value: 1000000000
globalDefault: false
preemptionPolicy: PreemptLowerPriority
description: "High priority class for physical kiosks to prevent eviction. We need to run on charlie"
