apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-energy
  namespace: {{ .Release.Namespace }}
  annotations:
    grafana_dashboard_folder: /tmp/dashboards/Energy
  labels:
    grafana_dashboard: "1"
data:
  {{- (.Files.Glob "dashboards/energy/**.json").AsConfig | nindent 2 }}
