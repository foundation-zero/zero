[project]
name = "zero-hull-temperature"
version = "0.1.1"
description = ""
readme = "README.md"
authors = [
    { name = "Boudewijn van Groos", email = "boudewijn.vangroos@foundationzero.org>" }
]
requires-python = ">= 3.13.1,<4.0"
packages = [{ include = "zero_hull_temperature"}]
dependencies = [
    "aiomqtt (>=2.4.0,<3.0.0)",
    "jsonpath-ng (>=1.7.0,<2.0.0)",
    "pydantic (>=2.11.7,<3.0.0)",
    "pydantic-settings (>=2.10.1,<3.0.0)",
    "pymodbustcp (>=0.3.0,<0.4.0)",
    "tenacity (>=9.1.2,<10.0.0)"
]

[tool.poetry.group.dev]
optional = true

[tool.poetry.group.dev.dependencies]
ruff = "^0.12.7"
pyright = "^1.1.403"

[tool.poetry.group.test]
optional = true

[tool.poetry.group.test.dependencies]
pytest-asyncio = "^1.1.0"
pytest = "^8.4.1"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_default_fixture_loop_scope = "function"
asyncio_mode = "auto"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
