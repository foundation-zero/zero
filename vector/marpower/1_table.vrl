topic_stem = join!(slice!(split!(.topic, "/"), 0, -1), "/")
.table = if includes(grouped_topics, topic_stem + "/") { topic_stem } else { .topic }
.table = replace(replace!(.table, "/", "__"), "-", "_")
