table:
  name: sails
  schema: loads
object_relationships:
  - name: sailPosition
    using:
      foreign_key_constraint_on: position_id
array_relationships:
  - name: sailSets
    using:
      foreign_key_constraint_on:
        column: sail_id
        table:
          name: sail_sets
          schema: loads
