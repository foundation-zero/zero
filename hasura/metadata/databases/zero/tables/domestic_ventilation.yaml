table:
  name: ventilation
  schema: domestic
object_relationships:
  - name: room
    using:
      foreign_key_constraint_on: id
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - actual_co2
        - co2_setpoint
      filter:
        id:
          _eq: X-Hasura-Cabin
    comment: ""
