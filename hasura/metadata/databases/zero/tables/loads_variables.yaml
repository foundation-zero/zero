table:
  name: variables
  schema: loads
array_relationships:
  - name: referenceValues
    using:
      foreign_key_constraint_on:
        column: variable_id
        table:
          name: reference_values
          schema: loads
