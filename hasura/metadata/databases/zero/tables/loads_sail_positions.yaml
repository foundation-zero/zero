table:
  name: sail_positions
  schema: loads
array_relationships:
  - name: sailSets
    using:
      foreign_key_constraint_on:
        column: position_id
        table:
          name: sail_sets
          schema: loads
  - name: sails
    using:
      foreign_key_constraint_on:
        column: position_id
        table:
          name: sails
          schema: loads
