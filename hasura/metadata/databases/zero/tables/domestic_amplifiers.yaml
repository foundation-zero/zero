table:
  name: amplifiers
  schema: domestic
object_relationships:
  - name: room
    using:
      foreign_key_constraint_on: id
select_permissions:
  - role: user
    permission:
      columns: '*'
      filter:
        id:
          _eq: X-Hasura-Cabin
    comment: ""
